{
  "name": "ix-opencode-plugin",
  "version": "1.0.0",
  "description": "OpenCode plugin for Ix Memory — graph-first reasoning layer",
  "scripts": {
    "test": "bun test",
    "test:live": "IX_LIVE_TESTS=1 bun test"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.4.2"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
